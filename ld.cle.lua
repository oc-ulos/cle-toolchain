-- LD.CLE - the CLE linker --
-- This linker will look through file data for any `require`s that
-- may be present. It will link the program against those libraries.

local function read_link_requests(fd)
end
